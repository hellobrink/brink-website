---
import { withBase } from '../lib/base';

interface Props {
  name?: string;
  role?: string;
  photo?: string;
  sectorName: string;
}
const { name, role, photo, sectorName } = Astro.props;
---

<section class="named-cta">
  <div class="container cta-inner">
    {photo && <img src={withBase(photo)} alt={name} class="cta-photo" />}
    <div>
      <p class="mono">Talk to us about {sectorName}</p>
      <h3>{name ?? 'TODO: named contact for this sector'}{role && `, ${role}`}</h3>
    </div>
    <a class="button" href="mailto:coffee@hellobrink.co">Get in touch</a>
  </div>
</section>

<style>
  .named-cta {
    background: var(--color-surface-muted);
    border: var(--border-width) solid rgba(6, 51, 61, 0.15);
    border-radius: var(--radius);
    padding: var(--space-6);
    margin: var(--space-8) auto;
    max-width: var(--max-width);
  }

  .cta-inner {
    display: flex;
    align-items: center;
    gap: var(--space-5);
    flex-wrap: wrap;
    padding: 0;
  }

  .cta-photo {
    width: 64px;
    height: 64px;
    border-radius: var(--radius-circle);
    object-fit: cover;
  }

  .cta-inner h3 {
    margin: 0;
  }

  .mono {
    font-family: var(--font-mono);
    text-transform: uppercase;
    font-size: var(--text-tag);
    color: var(--color-text-muted);
  }

  .cta-inner .button {
    margin-left: auto;
  }
</style>
